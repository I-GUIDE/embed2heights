# Legacy Presets

Keep old YAML notes here only as documentation. Legacy model branches have
been removed from live code; restore them from git only if an old experiment
becomes competition-relevant again.
