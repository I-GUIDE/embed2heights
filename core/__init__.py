"""Core package for the active emb2heights pipeline.

High-level entrypoints:
- ``core.models``: the active model, its components, and the build factory.
- ``core.config``: YAML recipe and CLI configuration helpers.
- ``core.data``: raster IDs, discovery, splits, datasets, and loader assembly.
- ``core.engine``: device, checkpoint, seed, and train-loop runtime helpers.
- ``core.metrics`` / ``core.losses``: task contracts.
"""
